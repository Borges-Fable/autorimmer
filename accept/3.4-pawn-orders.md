# Acceptance — spec 3.4, Pawn orders + policies (git-bug `39c9db7`)

The worker that wrote 3.4 may never launch RimWorld, so this is the acceptance in
executable form. `accept/3.4-pawn-orders.ps1` runs every step below and prints
`PASS`/`FAIL` per numbered check; this file is the same sequence written out, so
it can be driven by hand, by `rwa`, or by any other client.

```
pwsh accept\3.4-pawn-orders.ps1                 # everything: 76 envelopes + one `pawn` read per colonist, 140 checks
pwsh accept\3.4-pawn-orders.ps1 -Phase 3        # one phase (phase 0 always runs first)
pwsh accept\3.4-pawn-orders.ps1 -DryRun         # print the plan and every expectation, send nothing
pwsh accept\3.4-pawn-orders.ps1 -Root <path>    # a protocol root elsewhere
```

Exit `0` = every check passed · `1` = at least one FAIL · `2` = a fixture
precondition could not be met, **which is not a spec failure** and says so.

## Why PowerShell and the raw file protocol, not `rwa`

**BORGES has no python** — only the Microsoft Store alias stub. RUNLOG session 5
already records it ("the Windows layout line stays unexercised until python
lands"), and `rwa` is python3. An acceptance script that cannot run on the box
that has the bench is worse than none, so this driver writes
`commands/<id>.json` and reads `results/<id>.json` directly. It is the same
protocol `rwa` speaks; on a box with python, every envelope below is
`rwa <op> --args-json '<json>'`.

Ids are kept to `[A-Za-z0-9-]` so `Poller.Sanitize` leaves them alone and the
result file is exactly `<id>.json`.

## Fixture: what the bench must already have

The script checks each of these and exits `2` with the reason if one is missing.
None of them is 3.4's to build.

| # | needs | why | stage it with |
|---|---|---|---|
| 0.3b | ≥ 2 visible colonists | an actor and a patient | `dev:starter-kit`, or a save |
| 0.3f | **at least ONE colonist capable of `Hauling` AND `Doctor`** — the script now finds it rather than assuming it | bullet 2 orders a haul and bullet 4 needs the actor to operate. A colonist with `Hauling` disabled fails SIX checks (2.3b, 2.4a/c/d/e, 2.5b) whose only clue is a NOTE reading "work type disabled for this pawn: hauling" | nothing, if any colonist qualifies. Phase 0 reads `pawn {sections:["work"]}` `disabled` for every colonist and takes the first that passes, naming every rejected pawn and why |
| 4.9 | **a doctor who is NOT the patient, with `Doctor` CHECKED** | a pawn cannot operate on itself, and skill is not assignment. With manual priorities off the Work tab is a checkbox column: `0` is unchecked, `3` is checked. If the only pawn with Doctor at 3 is the patient, the bill sits forever, nobody walks to the bed, and NOTHING reports an error | `work-priorities {set:[{pawns:[…],works:["Doctor"],priority:3}]}` — 3 is legal either way; 1, 2 and 4 need manual priorities on, which step 0.5 now turns on |
| 4.7, 5.1 | **manual work priorities ON** | both use priorities 1 and 2, which are meaningless — and correctly refused — while `GetPriority` returns a flat 3. This is no longer a fixture the run has to be handed: step 0.5 stages it | `work-priorities {manual:true}` (git-bug `e8f2c32`) |
| 3.5 | **rested pawns** | `JobGiver_OptimizeApparel` only runs at a free think tick. A pawn that goes to sleep first never re-dresses and bullet 3 fails on a 20000-tick timeout | `dev:set-need {need:"Rest",val:1.0}` on the roster before the run |
| 2.2 | **the stockpile sited AWAY from where colonists idle** | phase 2 spawns the steel at `pawn:A`. If the zone is on top of the pawns the stack lands already-stored, `HaulGeneral` correctly yields no job, and bullet 2 cannot run | site it 15+ cells from the colony centre and confirm with `find-rect` that it is still reachable |
| 1.1 | a buildable, reachable cell near the actor | somewhere to be ordered to | any colony |
| 2.1 | ≥ 1 stockpile zone | `WorkGiver_HaulGeneral` yields no job with no storage destination | 3.2's zone verbs, or a save |
| 4.4c | `Anesthetize` offered on the patient | the fixture surgery | Core; any flesh humanlike |
| 5.9 | ≥ 1 startable research project | `research-set` needs a target | any colony with a bench |
| 5.16 | a visible hostile faction | a prisoner for the warden verbs | any normal world (skipped with a NOTE if absent) |

> **THE ACTOR IS SELECTED, NOT INDEXED — and that is now two separate fixes.**
> The original text of this box said `pawns {filter:"colonist"}` does not
> promise a stable order (it returned a different order for the same colony
> across two calls in one session, 2026-08-31) and told you to make **every**
> colonist suitable, because "make the first colonist suitable" was not a thing
> you could arrange. Both halves have been dealt with, and they were different
> problems wearing one costume:
>
> * **The order is now promised** (git-bug 1eb2262). `pawns` emits by
>   `thingIDNumber` ascending and publishes `order:"id-asc"` beside
>   `selected_by:"attention-desc"`; phase 0 asserts both (0.3c, 0.3d) and
>   NOTEs (0.3e) if the ids come back unsorted, which means an assembly older
>   than that fix is loaded.
> * **The actor is no longer an index.** Phase 0 now reads
>   `pawn {sections:["work"]}` for every colonist and takes the first whose
>   `work.disabled` contains neither `Hauling` nor `Doctor` (0.3f); **B** is
>   simply the first colonist that is not **A** (0.3g), which satisfies 4.9's
>   "a doctor who is not the patient" by construction. Colonists that did not
>   qualify are printed with the reason (0.3h).
>
> A stable order makes an index *reproducible*; it does not make it
> *meaningful*. The over-constrained fixture — "make every colonist suitable"
> — is no longer required, and a fixture with one specialist now works.

Everything else the script stages itself with `dev:*` verbs only — 3.1's layer,
per the dispatch. **No fixture verbs and no fixture steps were added by 3.4.**

---

## Phase 0 — preflight

| # | envelope | expected |
|---|---|---|
| 0.1 | `{"op":"status"}` | `ok:true`, `data.gameLoaded:true`, `data.paused:true`, **no** `data.forcePause` (spec 1.7: a modal wedges every later advance) |
| 0.2 | (same result) | `data.verbs` contains all 35 of 3.4's ops |
| 0.3a/b | `{"op":"pawns","args":{"filter":"colonist"}}` | `ok:true`, ≥ 2 entries |
| 0.3c/d | (same result) | `data.order` is `"id-asc"` and `data.selected_by` is `"attention-desc"` — the order contract of git-bug 1eb2262 |
| 0.3e | (same result) | NOTE only: the ids are ascending. If they are not, the loaded assembly predates 1eb2262 |
| 0.3f | `{"op":"pawn","args":{"id":…,"sections":["work"]}}` per colonist | some colonist has neither `Hauling` nor `Doctor` in `data.work.disabled`; the first such (in id order) becomes **A** (actor) |
| 0.3g | (same) | the first colonist that is not **A** becomes **B** (patient) |
| 0.4 | `{"op":"journal","args":{"limit":1}}` | `ok:true`; `data.last_seq` becomes `seq0`, the watermark every later journal assertion is scoped to |
| 0.5 | `{"op":"work-priorities","args":{"manual":true}}` | `data.manual.after:true`. **Staged, not asserted** — 4.7 and 5.1 use priorities 1 and 2, which `work-priorities` refuses by design while the Work tab is a checkbox column. `PlaySettings.useWorkPriorities` scribes `defaultValue:false`, so before git-bug `e8f2c32` those eight checks were unreachable on any colony the agent staged itself. The verb's own acceptance is `accept/manual-work-priorities.md` |

---

## Phase 1 — bullet 1: **draft + move + undraft round-trip; the drafted pawn holds position**

| # | envelope | expected |
|---|---|---|
| 1.1 | `{"op":"find-rect","args":{"w":1,"h":1,"near":"pawn:A","max":5,"require":["buildable","reachable-from:pawn:A"]}}` | ≥ 1 candidate; the farthest becomes `DEST` |
| 1.2 | `{"op":"draft","args":{"pawns":[A]}}` | `counts.accepted:1`, `counts.rejected:0`, `accepted[0].drafted:true`, `drafted_before:false`, `priority_work_cleared:true`, `action.journal_seq ≥ 1`, and **no `cheat` key** on the action block (an `action` is not a `dev` row) |
| 1.3 | `{"op":"pawn","args":{"id":A,"sections":["state"]}}` | `state.drafted:true` — the observer and the actor agree, one vocabulary |
| 1.4 | `{"op":"move-to","args":{"pawns":[A],"to":DEST}}` | `counts.accepted:1`, `accepted[0].job_def:"Goto"`, `action.journal_seq ≥ 1`; `standable_near` recorded |
| 1.5 | `{"op":"advance","args":{"ticks":2000,"max_tps":600}}` | `data.reason:"ticks"` (anything else means a dialog, a letter or a red error interrupted) |
| 1.6 | `{"op":"pawn","args":{"id":A,"sections":["state"]}}` | `state.at` within **3 cells** of `standable_near` (`RCellFinder.BestOrderedGotoDestNear` spreads), `state.drafted:true` |
| 1.7 | `advance {ticks:4000}` then `pawn` again | `state.at` **identical** to 1.6 — the drafted pawn HELD; `state.drafted:true`; `state.job_def` ∈ {null, `Wait_Combat`, `Wait`, `Attack*`, `Goto`} |
| 1.8 | `{"op":"undraft","args":{"pawns":[A]}}` | `counts.accepted:1`, `accepted[0].drafted:false`, `action.journal_seq ≥ 1` |
| 1.9 | `{"op":"undraft","args":{"pawns":"colonists"}}` | `ok:true`; every rejection's `gate` is `"already"` — the whole-roster loop-closing form is one call |
| 1.10 | `{"op":"journal","args":{"since_seq":seq0,"types":["action"],"limit":200}}` | `payload.verb` includes `draft`, `move-to`, `undraft` |
| 1.11 | `{"op":"journal","args":{"since_seq":seq0,"types":["red_error"]}}` | `data.count:0` — the standing invariant |

**Advance windows.** 2000 ticks is generous for a walk across a colony
(`Goto` at ~0.1s/cell). The hold check is 4000 further ticks, chosen because a
colonist's next need (food, rest, joy) would surface well inside it if the draft
were not holding.

---

## Phase 2 — bullet 2: **`prioritize haul` on a specific stack; that pawn hauls it next**

| # | envelope | expected |
|---|---|---|
| 2.1 | `{"op":"zones","args":{"kind":"stockpile"}}` | `data.stockpiles.total ≥ 1` (precondition) |
| 2.2 | `{"op":"dev:spawn-thing","args":{"def":"Steel","count":75,"pos":"pawn:A","stockpile":false}}` | `ok:true`, `data.placed ≥ 1`; `data.spawned[0].id` becomes `STEEL`. On the GROUND, not in storage, so hauling it is a real job |
| 2.3 | `{"op":"orders","args":{"pawn":A,"thing":STEEL}}` | `ok:true`; `data.available[].work` contains `"HaulGeneral"` — the parity list, as wide as the bench's WorkGiver set because `WorkGiverDef.directOrderable` defaults true |
| 2.4 | `{"op":"prioritize","args":{"pawn":A,"work":"HaulGeneral","thing":STEEL}}` | `data.ok:true`, `data.work:"HaulGeneral"`, `data.job_def` starts `HaulTo`, `action.journal_seq ≥ 1`, `data.sustains` is a **bool**. If `sustains:true` then `priority_work.active:true` and `priority_work.work_giver:"HaulGeneral"`; if false the result says nothing durable was written |
| 2.5 | `advance {ticks:10, max_tps:60}` then `pawn {id:A,sections:["state"]}` | `state.job_def` starts `HaulTo` — **the bullet, literally**: `TryTakeOrderedJob` ends the current job and enqueues first, so the haul is the job taken on the next tick |
| 2.6 | `advance {ticks:4000}` then `pawn` | `state.job_def` no longer starts `HaulTo` — it completed |
| 2.7 | `{"op":"clear-priority-work","args":{"pawns":[A]}}` | `ok:true` (accepted, or refused with a named gate) |
| 2.8 | red-error sweep | `data.count:0` |

**Why the assertion is on the JOB and not on the steel's final position.** Hauled
stacks MERGE into an existing stack of the same def, which destroys the hauled
`Thing` — so `thing:STEEL` can legitimately stop existing. The acceptance bullet
says "that pawn hauls it next", and 2.5 is that sentence exactly.

---

## Phase 3 — bullet 3: **a "cold" outfit policy assigned; the pawn re-dresses under advance**

| # | envelope | expected |
|---|---|---|
| 3.1 | `dev:spawn-thing {def:"Apparel_Parka",stuff:"Cloth",count:1,stockpile:true,quality:"Normal"}`, then the same for `Apparel_Tuque` | `ok:true`, `data.placed ≥ 1` each. **`stockpile:true` is not a nicety**: `JobGiver_OptimizeApparel` skips any candidate failing `IsInAnyStorage()` |
| 3.2 | `{"op":"pawn","args":{"id":B,"sections":["apparel"]}}` | records `worn[].def` and `insulation_cold_total` as the BEFORE; asserts the parka is not already worn |
| 3.3 | `{"op":"policy-new","args":{"kind":"apparel","label":"cold","disallow_all":true,"allow":["Apparel_Parka","Apparel_Tuque"]}}` | `ok:true`, `data.label:"cold"`, `action.journal_seq ≥ 1`, `data.edits` contains `allow:Apparel_Parka`, `data.refused` empty (nothing fell outside the apparel global filter); `data.id` becomes `POLICY` |
| 3.4 | `{"op":"assign","args":{"pawns":[B],"apparel_policy":POLICY}}` | `counts.accepted:1`, `accepted[0].applied` contains `apparel_policy`, `accepted[0].after.apparel:"cold"`, `accepted[0].after.source:"backing-field"` (the read went through PawnSafe's guarded route, not the getter that assigns and scribes), `action.journal_seq ≥ 1` |
| 3.5 | `advance {ticks:5000, max_tps:600}` | `data.reason:"ticks"`. If the parka is not yet worn, the script advances a documented **fallback** 15000 more (20000 total) and says which window it took |
| 3.6 | `{"op":"pawn","args":{"id":B,"sections":["apparel"]}}` | `worn[].def` contains `Apparel_Parka`; `insulation_cold_total` **greater** than 3.2's; at least one previously-worn item gone (`JobGiver_OptimizeApparel`'s `RemoveApparel` pass) |
| 3.7 | `{"op":"policies"}` | `data.outfits.list[].label` contains `"cold"`, and `data.assignments` shows B on `"cold"` — 2.4's observer and 3.4's act, one vocabulary |
| 3.8 | `{"op":"policy-delete","args":{"kind":"apparel","policy":POLICY}}` | `data.ok:false` with a non-empty `data.reason` — the database's own `AcceptanceReport` string naming the pawn still using it |
| 3.9 | red-error sweep | `data.count:0` |

### The spec's implied mechanism is wrong, and this is the honest version

The spec bullet reads as "make it cold → the pawn dresses warm". **Weather cannot
drive this loop.** `JobGiver_OptimizeApparel` computes `neededWarmth` from
`PawnApparelGenerator.CalculateNeededWarmth`, which is
`GenTemperature.AverageTemperatureAtTileForTwelfth(tile, twelfth)` — the tile's
**seasonal average**, not the live weather. `dev:weather` moves rain, wind and
accuracy; it does not move `neededWarmth` by one degree.

What DOES drive the loop, deterministically, is the **policy filter** — which is
what the bullet is really testing ("verifies the whole clothes loop end to end"):

1. `Notify_OutfitChanged` (fired by the apparel setter) sets
   `nextApparelOptimizeTick = now`, so the pawn re-optimizes at its next free
   think tick instead of in 6000–9000.
2. The first loop in `JobGiver_OptimizeApparel` issues `RemoveApparel` for every
   worn item the new filter disallows — unconditional, no score maths.
3. `ApparelScoreGain` then multiplies a non-conflicting candidate's score by
   **10**, so a parka on a now-bare torso is a large positive gain, well over the
   `MinScoreGainToCare` floor of 0.05.

So the fixture is "a policy that allows only warm apparel, with that apparel in
storage", and the mechanism is filter-driven and season-independent. Flagged on
the issue.

**Why the fallback window.** `nextApparelOptimizeTick = now` makes the pawn
eligible immediately, but it still has to be free: asleep, eating, in a mental
state or mid-job all delay the think pass. 5000 ticks covers the ordinary case;
20000 covers a sleeping pawn.

---

## Phase 4 — bullet 4: **a surgery bill added; a doctor performs it under advance**

| # | envelope | expected |
|---|---|---|
| 4.1 | `dev:spawn-thing {def:"HospitalBed",count:1,pos:"pawn:B",faction:"player"}` | `ok:true`, `placed ≥ 1`; `spawned[0].id` becomes `BED`. `HospitalBed`'s def carries `bed_defaultMedical:true`, so it is Medical without a toggle verb (toggling Medical on an ordinary bed is not 3.4's surface) |
| 4.2 | `dev:spawn-thing {def:"MedicineHerbal",count:10,stockpile:true}` | `placed ≥ 1` — `Anesthetize` takes one Medicine |
| 4.3 | `dev:damage {pawn:B,mode:"amount",amount:7,hits:2,def:"Cut"}` then `pawn {sections:["state","health"]}` | `state.downed:false`, `health.hediffs_total ≥ 1` — wounded but STANDING, which is what makes `rest-until-healed` legal (`HealthAIUtility.ShouldSeekMedicalRest`) |
| 4.4 | `{"op":"surgery-options","args":{"pawn":B,"cap":200}}` | `ok:true`, `data.source:"backing-field"` (research read through the guarded route, never `GetProgress`); exactly one `Anesthetize` option, `addable:true` |
| 4.5 | `{"op":"surgery-add","args":{"pawn":B,"recipe":"Anesthetize"}}` | `data.ok:true`, `action.journal_seq ≥ 1`, `data.bills[].recipe` contains `Anesthetize`, `data.warnings` is a **list** — the four `CreateSurgeryBill` warnings returned rather than messaged, because one of them (`Bill.CreateNoPawnsWithSkillDialog`) is a force-pausing `Dialog_MessageBox` |
| 4.6 | `{"op":"bills","args":{"bench":B}}` | a bench entry with `kind:"pawn"` whose `bills[].recipe` contains `Anesthetize` — 2.4's observer sees 3.4's write |
| 4.7 | `{"op":"work-priorities","args":{"set":[{"pawns":[A],"work":"Doctor","priority":1}]}}` | `ok:true`, `data.cells:1`, `data.counts.unit:"matrix cells"`, `data.changes[0].after:1`, `action.journal_seq ≥ 1`. `data.use_priorities` is printed and is `true` — step 0.5 turned it on; with it OFF this call is REFUSED, because `GetPriority` would return a flat 3 |
| 4.8 | `{"op":"rest-until-healed","args":{"pawns":[B],"bed":BED}}` | `counts.accepted:1`, `action.journal_seq ≥ 1`. Needed because a pawn with only a BILL does not go to bed on its own — `WorkGiver_PatientGoToBedTreatment` requires `ShouldSeekMedicalRestUrgent` |
| 4.9 | `advance {ticks:6000}` then `bills {bench:B}` | the `Anesthetize` bill is **gone** — `Bill_Medical` deletes itself on completion. Fallback: 14000 more (20000 total) |
| 4.10 | `{"op":"pawn","args":{"id":B,"sections":["health"]}}` | `health.hediffs[].def` contains `Anesthetic` |
| 4.11 | `surgery-add` again, then `surgery-remove {pawn:B,recipe:"Anesthetize"}` | `data.ok:true` on the remove, `action.journal_seq ≥ 1` (skipped with a NOTE if the re-add is refused because the pawn is already anesthetized) |
| 4.12 | red-error sweep | `data.count:0` |

**Why `Anesthetize`.** Core, `workAmount 0`, one Medicine, `targetsBodyPart:false`
and `addsHediff:null` — so it is offered on any flesh humanlike, has no
body-part ambiguity, needs no research, and completes the instant the doctor
reaches the patient. It is the cheapest deterministic surgery in the game, which
is exactly what a fixture wants.

**The advance window.** The doctor has to path to the patient and the patient has
to reach the bed; 6000 ticks (~2.7 in-game hours) is comfortable for a small
colony. 20000 covers a doctor across a large base or one that stops to eat.

---

## Phase 5 — the rest of the surface: the plural forms, the gates that refuse, research

| # | envelope | expected |
|---|---|---|
| 5.1 | `work-priorities {set:[{pawns:[A,B],works:["Doctor","Firefighter"],priority:2}]}` | `ok:true`, `data.cells ≥ 1`, `data.mode:"matrix"` — **up to four cells in ONE call**, the cross-product form. Priority 2 needs 0.5’s manual flip; without it this is refused, correctly |
| 5.2 | `work-priorities {copy_from:A,to:[B]}` | `ok:true`, `data.mode:"copy"`, `accepted[0].set ≥ 1` — a whole row in one call (amendment item 7) |
| 5.3 | `work-priorities {set:[{pawns:[A],work:<a disabled type>,priority:3}]}` | `counts.accepted:0`, `rejected[0].gate:"work-disabled"`, **and no red error** — `Pawn_WorkSettings.SetPriority` answers a disabled work type with `Log.Error`, which this pre-check exists to prevent. Skipped with a NOTE if the colonist has no disabled type |
| 5.4 | `schedule {pawns:[A,B],hours:"22-3",assignment:"Sleep"}` | `ok:true`, `data.hours:[22,23,0,1,2,3]` — a **wrapping span** is one call; `accepted[0].row` is 24 chars with `S` at hours 22 and 0, in `PawnSerializer.Schedule`'s own A/W/J/S/M legend; `action.journal_seq ≥ 1` |
| 5.5 | `schedule {pawns:[B],copy_from:A}` | `ok:true` |
| 5.6 | `assign {pawns:[A,B],med_care:"NormalOrWorse",self_tend:true,hostility:"Flee",area:"Home"}` | `counts.accepted:2`; `after.med_care:"NormalOrWorse"`, `after.hostility_response:"Flee"`, `after.area:"Home"` (`Area_Home` overrides `AssignableAsAllowed` to true, so this needs nothing from 3.2); `respects_area` published as a bool |
| 5.7 | `assign {pawns:[A,B],area:null}` | `counts.accepted:2`, `after.area:null` — unrestricted is a real setting, not an omission |
| 5.8 | `assign {pawns:[A],area:"no-such-area-xyz"}` | `ok:false`, and `error.detail` lists the assignable areas **and names spec 3.2** as the owner of area creation |
| 5.9 | `research {cap:5}` then `research-set {project:<first available>}` | `data.ok:true`, `data.current` == that project (**read back**, not asserted), `action.journal_seq ≥ 1`, `data.source:"backing-field"` |
| 5.10 | `research-set {project:"ShipBasics"}` | if unstartable: `data.ok:false`, `data.blocked_by` ∈ {`finished`,`prerequisites`,`techprints`,`no-bench`,`mechanitor`,`analysis`,`codex-hidden`,`grav-engine`} — 2.4's own `blocked_by` vocabulary — and `data.note` names `baseCost`, i.e. says plainly that `SetCurrentProject` would have accepted it. Skipped with a NOTE if it happens to be startable |
| 5.11 | `research-stop {}` | `data.ok:true` |
| 5.12 | `undraft {pawns:[A]}` then `tend {pawn:A,target:B}` | `counts.accepted:0`, `rejected[0].gate:"drafted-only"`, `action.journal_seq:null` — **the gate rule, demonstrated**: an ignored `Drafted` flag would have silently no-opped |
| 5.13 | `draft {pawns:[A]}` then the same `tend` | accepted, or refused for a substantive reason — never `drafted-only` again |
| 5.14 | `fire-at-will {pawns:[A],on:false}` | accepted, or a named gate ∈ {`not-drafted`,`no-ranged-weapon`,`no-drafter`,`already`} — never a silent no-op |
| 5.15 | `warden {pawns:[B],enable:["AttemptRecruit"]}` | `ok:false`, and `error.detail` mentions the **red error** it prevented — `ToggleNonExclusiveInteraction` `Log.ErrorOnce`s on an exclusive def |
| 5.16 | `dev:spawn-pawn {kind:"Colonist",faction:"hostile",pos:"pawn:A"}` → `dev:guest-status {status:"prisoner"}` → `warden {mode:"AttemptRecruit",recruitable:true}` → `warden {mode:"Release"}` | `counts.accepted:1`; `after.interaction` is `"AttemptRecruit"` then `"Release"`; `modes_available.mode` contains `Release`. **Release is a MODE**, not a `Released` flag write |
| 5.17 | `dev:damage {pawn:B,...}` → `undraft {pawns:[A]}` → `prioritize {pawn:A,work:"DoctorTendToHumanlikes",thing:B}` | `data.ok:true`, **`data.sustains:true`**, `priority_work.active:true`, `priority_work.work_giver:"DoctorTendToHumanlikes"`, `priority_work.timeout_ticks:30000` — **the durable-state write**, which phase 2 could not exercise. Then `draft`/`undraft` and `clear-priority-work` shows it was cleared by the draft setter. Skipped with a NOTE if the option is not offered |
| 5.18 | red-error sweep + `status` | `data.count:0`, and **no** `data.forcePause` left behind |

---

## What this script does NOT prove

- **It has never been run.** The author may not launch RimWorld. Every expected
  value is derived from the decompiled 1.6 source in
  `misc/rimworld/reference/decompiled/RimWorldBase` and from the shipped
  serializers' field names — not from an observed run.
- **It is not a coverage sweep.** 35 verbs ship; this exercises about 25 of them.
  `attack`, `rescue`, `capture`, `carry`, `equip`, `drop`, `consume`,
  `extinguish`, `beat-fire`, `repair`, `man-turret`, `policy-edit` and
  `policy-default` have no step here — each needs a fixture (a hostile in melee
  range, a downed pawn, a live fire, a damaged building, a turret) that is more
  staging than the four acceptance bullets ask for. Their gates are cited in code
  and their reject paths are the same shape as the ones phase 5 does exercise.
- **Timing windows are estimates, not measurements.** Where a window is chosen,
  the reasoning is written above it and a fallback window is attempted before the
  check fails, so "too slow" is distinguishable from "never happened".
- **`prioritizeSustains` for `HaulGeneral` is asserted as a bool, not as `true`.**
  Core does not set it on that work giver, so phase 2 verifies only the result's
  honesty about *not* writing (`sustains:false` + "no standing order was
  written"). The write itself is exercised at 5.17 with
  `DoctorTendToHumanlikes`, which does set it — in Core the sustaining set is the
  doctor, bill-worker and construction givers (`DoBills*`, `ConstructFinishFrames`,
  `Construct/DeliverResourcesTo*`, `FixBrokenDownBuilding`, `Refuel`,
  `RearmTurrets`, `Train`, `TakeToPen`), **not** mining or sowing, which is worth
  knowing before writing another test against a guess.
