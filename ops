{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788224793,"nonce":"kmMN5YBIdtUOm513ZVXLc8Td4qg=","message":"GREEN — closed on live acceptance, session 11 (`65b03c2`), 2026-08-31.\n\n`accept/8b0b88f-already-designated.py` — **all 123 checks passed, exit 0.** Log at `accept/runs/s11-20260831/`.\n\nThe merged fix does what this issue asked. Measured live rather than inferred: a second `designate` over the same rect returns `rejects_by_reason = {\"not-designatable\": 142, \"already-designated\": 2}`, so a redundant order is now distinguishable from an impossible one, which was the whole complaint.\n\n**The targetType hazard is closed and proven closed.** All four quadrants are driven and then a `red_error` read is taken over a window containing only those four calls: `mine --cells` → `DesignationAt(cell)`; `mine --things` → `DesignationAt(thing.Position)`; `cut --things` → `DesignationOn(thing)`; `cut --rect` → `GetThingList` + `DesignationOn`. The middle two are the ones this issue's text does not name and a per-verb accessor gets wrong. Zero red errors.\n\nHunt's `\"MessageMustDesignateHuntable\"` misattribution is kept verbatim under a corrected `why`, and 5.3a/5.3b assert exactly that — so the third acceptance bullet is satisfied by the 'documented at the call site' branch, not the 'corrected' one.\n\n**One defect the suite found in itself, worth recording.** Its first run was 121/123, and both failures were its own: `1.2l`/`1.2m` dug `data.action.rejected_by_reason` on the RESPONSE, where `action` is `{journal_seq}` and nothing else. The tally has two spellings in two places — `rejects_by_reason` on the data block (`DesignateEngine.PublishRejects`), `rejected_by_reason` on the JOURNAL ROW (`DesignationVerbs.Designate`). The dig returned an ABSENT key reading as null. That is precisely the failure mode this suite exists to close, reproduced inside the suite. Fixed to read the journal at the seq the action block names, which is the stronger assertion anyway.","files":null}]}