{"author":{"id":"e0b5a45ee94df92761e1991b03f72f45cac91cde564e2faac1223bf52a3fe292"},"ops":[{"type":3,"timestamp":1788125034,"nonce":"C3+yzqIs9BGgGlupjOFHf+ntGrc=","message":"ACCEPTANCE — verified personally on the BORGES bench, 2026-08-30. All four criteria met; `state:done`.\n\nShips: Spatial.cs (Positions resolver, LandmarkComponent persisted via GameComponent ExposeData, CropRenderer — the single crop source), SpatialVerbs.cs (`map-view`, `find-rect`, `nearest`, `reachable`, `room-at`, `path-cost`, `landmark`). Every position argument goes through one resolver: `\"x,z\"` | `[x,z]` | landmark | `pawn:\u003cid\u003e` | `thing:\u003cid\u003e`.\n\n**1. 5x7 room corner placed sight-unseen, queries only, verified valid.**\n\nThe scripted chain, zero coordinate arithmetic (the only numbers passed were copied verbatim from prior results):\n\n```\nnearest {category:food}                  -\u003e meal pile id 2689 at [114,133]\nlandmark {set:{name:drop, at:\"thing:2689\"}}   (returns the crop echo)\nfind-rect {w:5,h:7, near:drop, require:[buildable, reachable-from:drop]}\n   -\u003e candidates[0] at [113,132] (dist 2.2), rejected {}\nmap-view {rect:[113,132,5,7]}            -\u003e independent render path: every cell\n   terrain/plant/item glyph, no '#' '%' '~' — clear footprint (trees/items are\n   blueprint-clearable by game rules)\nreachable {from:drop, to:\"113,132\"}      -\u003e true\npath-cost {from:drop, to:\"113,132\"}      -\u003e found, cost 32\n```\n\n`buildable` = Heavy terrain affordance + no edifice (walls and every normal building carry on Heavy); rejections tally by first-failed requirement so scarcity comes back with reasons. Candidates sort by center distance (a ring-order/score mismatch was caught in the first run and fixed).\n\n**2. Edge crops verified at all four map edges** (250x250 map): `[-5,-5,12,12]` -\u003e origin [0,0] 7x7; `[245,-5]` -\u003e 5 wide; `[-5,245]` -\u003e z_top 249; `[245,245]` -\u003e 5x5 — all `clipped:true`, no wraparound, rulers honest (x tens+units strings, z_top/z_bottom per crop).\n\n**3. Landmark set -\u003e save -\u003e kill -\u003e load -\u003e resolves identically.** `drop:[114,133]` came back exact after a force-kill + autostart reload, and resolved in `find-rect` (same candidate set, now sorted), `room-at` (outdoors district, role none, 60653 cells) — the resolver treats a loaded landmark exactly like a fresh one.\n\n**4. Mutation echo shows correct before/after crops.** `landmark set` returns a crop through CropRenderer — the exact helper wave-3 verbs call — and the echo rendered the crash site legibly: colonists by name in the legend (`@ (Beans,Nicole)`), the tame animal, the meal pile, gear, trees, with per-crop legend listing only glyphs present. CropRenderer is the single rendering source by construction; there is no second code path to drift.\n\nGlyph/legend policy (the catalog.py substitution, per the dispatch note): glyphs derive deterministically from live def properties (impassable building '#', natural rock '%', door '+', tree 'T'/plant 't', item = lowercase label initial, building = uppercase initial, water '~', built floor '_', fertile ',', soil '.', roofed ':', zones '='/'\\\"', pawns '@'/'$'/'!'/'g'/'d'/'w'), stable per bench version because the def set is; colors remain the dumped baseviz_catalog.json's business (an ASCII channel has none). The catalog.py cross-check debt stands in RUNLOG's Needs Dorian.","files":null}]}