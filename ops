{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788227255,"nonce":"eCR/Q9hnrggfCqBRL7nJNm1xA/s=","message":"RECOVERED COMMENT — restored by session 11 (`65b03c2`) on 2026-08-31 after the git-bug merge breakage (`16b959a`).\n\nThis comment existed only on the parent that had to be dropped when this bug's ref was reset off an unreadable merge commit. It is reproduced verbatim below; its original id was `feb1b02`. The dropped parent is preserved on the remote at `refs/backup/s11-unreadable-gitbug-merge/fbb2c59`.\n\n---\n\n\nRESUMED 19:02. 2.5's mechanical acceptance RUN AND GREEN — and this was genuine\nfirst execution of `MapDumpVerbs.cs` and `CatalogDump.cs`, which had never run.\n\n- `catalog-dump` -\u003e ok, 3849 defs / 3465 things / 384 terrains, 951848 bytes to\n`\u003cprotocol root\u003e/catalog.json`. Worker predicted ~3800.\n- Two bench renders of the same rect: byte-identical, sha256\n`dd634fc1417a157f561c3714e62bde44b6b5e84137298ae7170cbac9ff1038ab`.\n- Offline replay from the saved dump: SAME sha256 as a fresh bench render. So\n\"same dump twice -\u003e byte-identical PNG\" now holds live, not only against\nfakebench.\n- Rect 51x51 from `pawn:19809`, 2601 cells, 6 rooms, 0 fogged, alphabet\n`baseviz-catalog/1`, image 862x1540 at scale 16.\n\n**Caveat, and it is a real gap rather than a pass:** this save has NO landmarks\nregistered (`landmark --list` -\u003e `{}`), so `base-center` does not resolve and\nthe worker's first call fails `bad-args` as written. I used the colonist\nfallback the worker specified. Consequence: `--landmarks` was a NO-OP here —\nrenders with and without it produce the identical hash — so **the landmark\nrendering path is untested, not proven.** `accept/2.5-png-render.md` should say\nso, and whoever stages a landmark should re-run.\n\nLegibility half is with a fresh agent that has seen only the PNG — the spec's\nactual acceptance, and the only part a contaminated reader cannot judge.\n\nBench up and paused at tick ~90371, `autostart.rws` reloaded clean.","files":null}]}