{"author":{"id":"e0b5a45ee94df92761e1991b03f72f45cac91cde564e2faac1223bf52a3fe292"},"ops":[{"type":3,"timestamp":1788137013,"nonce":"UPcIsGnsAxeRLq0gNs7QpWXIS9Y=","message":"CLOSED — acceptance verified personally by the orchestrator on the BORGES\nbench, session 5. Branch merged to main as a three-way merge (b2f037a; the\nparallel 2.2 branch fast-forwarded first), rebuilt on main, Build commit\nf15f69e. Bench session sid 20260831T003200 unless noted.\n\n\n1. **Kill a game mid-advance.** The `main-menu` fixture fired at t151605 with\n   `advance_in_flight:true` journaled; the in-flight `advance {ticks:2000000}`\n   got **exactly one** result file — `ok:false`, `no-active-game`, \"the game\n   was unloaded while this command was in flight\" — and the journal shows\n   `session {kind:\"unloaded\", aborted:5}`. Result latency ~25s, which is the\n   deliberate AbandonAfterSeconds window (judgement call below). Demonstrated\n   twice (second time while verifying the status fix, sid 20260831T004053).\n   **The \"subsequent load does NOT resume\" half is met in a weaker form, in\n   those words**: no programmatic load path exists before 3.1 (no load verb,\n   and autostart only fires at boot), so the same-process load-from-menu\n   variant was not driven. The LoadedGame edge shares the identical\n   GameBoundary → ResetForGameBoundary → TimeDriver.Abandon path with the\n   poller edge that WAS demonstrated; across a relaunch the statics are\n   trivially fresh and stale inbox files answer stale-on-restart (1.1's\n   demonstrated behaviour). Interlocked claim of `cmd` on every exit is what\n   makes double-answering impossible when both detectors fire.\n2. **Commands in the post-unload window.** 8 pings fired at 1/s across the\n   unload: **8/8 result files**, each exactly once, all `no-active-game` — 4\n   answered by the boundary reset (the aborted:5 = advance + 4 pending), the\n   rest by ScanInbox after the heartbeat window lapsed. On the unfixed build\n   the window's pings were consumed into done/ with zero results.\n3. **halt_on_error past the dedupe cap.** 9 `error` steps → the journal holds\n   exactly 4 red_error lines (3 plain + 1 suppressed marker). `error-at` then\n   fired the SAME text mid-advance: the advance halted `reason:\"red_error\"` at\n   ticks_elapsed 300 (of 20000 asked) with `halted_on.occurrence:10`,\n   `journal_suppressed:true`, `halted_seq:0` — and the journal STILL holds\n   exactly 4 lines. The cap survived as a file policy; the halt no longer\n   depends on it.\n4. **Hostile result payload.** `weird-result` produced exactly one result\n   file, valid JSON: null string → null, `[\"a\",null,\"c\"]` preserved, the\n   throwing ToString caught as `\u003cautorimmer: ThrowingToString.ToString()\n   threw InvalidOperationException\u003e`, NaN/Infinity → 0, the cyclic tree\n   terminated at MaxDepth, the Verse def serialized. The next command was\n   answered — the poller cycle survived, which was the second half of the\n   defect.\n\n\n- **Journal seq: 65 entries, zero gaps** across a session that included two\n  dialog storms, a 9-error storm, a mid-advance error, the weird-result\n  payload and the unload itself.\n- Zero unexpected red errors (4 selftest-induced, by exact text); Player.log\n  0 exceptions.\n\n\nThe 20s AbandonAfterSeconds window (vs the 5s GameLoaded window) stands: a\nstalled heartbeat also means \"inside a long event\", and falsely killing a\nhealthy advance is worse than answering an orphaned command ~20s late.\nObserved latency ~25s in both live runs.\n\n\nAfter the unload, status.json served the dead snapshot's\n`activeOp:\"advance:a1-adv\"` beside `gameLoaded:false`. Fixed on main\n(activeOp nulled when the game is gone), rebuilt, and verified with a second\nboot-unload cycle: `gameLoaded:false, activeOp:null`.\n\nDoc corrections (FINDINGS §9 letter/alert timing, JOURNAL.md schema rows +\ndialog section) merged with the branch. The worker's MiniJson standalone\nharness results are in its report above; I re-ran the build (0 warnings,\n0 errors) but not the harness.","files":null}]}