{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788190325,"nonce":"C6BVyyAz8v8ddEM4/n4+pmZR5Nk=","message":"BUILT, MERGED WITH MAIN, NOT YET VERIFIED IN GAME. Branch `spec/auto-arm-lever`\nat 87d1ccb. Stays state:doing.\n\nOrchestrator checks done so far (no bench needed):\n\n- Merged `main` into the branch after `seek-at-will` landed. **Clean merge, no\n  conflict**, despite both touching `PawnSerializer.cs` — seek adds `state.seek`,\n  auto-arm adds `auto_arm` to the area section, different regions.\n- Combined `-c Release` build: **0 warnings, 0 errors**. So the repo's two\n  mod-aware reflection bridges coexist without collision.\n- Read `FswaBridge.cs`'s header. It absorbed the SeekVerbs review lessons\n  properly: binds by signature AND return type then converts to a typed\n  delegate; `IsAutoArm` returns `bool?` where null means \"could not look\" rather\n  than the legal value `false`; reads the write back and treats disagreement as\n  a rejection; absence of the mod refuses the lever without failing the rest of\n  the `assign` call.\n\nTHREE THINGS THE WORKER FOUND THAT THE SPEC HAD WRONG, all trusting source over\nthe issue text, all worth keeping:\n\n1. **The gate has two widgets and the issue cited one.** The issue named the\n   gizmo. The gate that matters for a verb reproducing the Assign TAB is\n   `PawnColumnWorker_AutoArm.HasCheckbox` (`IsColonist \u0026\u0026 !WorkTagIsDisabled\n   (Violent)`). The gizmo additionally admits `MechUtility.IsWeaponUsableMech`\n   and skips the Violent check for mechs. The worker honours the union and cites\n   both — enforcing only the column would have invented a restriction the player\n   does not have.\n2. **The MpSync question had a different shape than implied.** `MpSync.RegisterAll`\n   registers `AutoArmTracker.SetAutoArm(Pawn,bool)` ITSELF with\n   `MP.RegisterSyncMethod`, because both FSWA callers funnel through that one\n   static setter. There is no separate wrapper to prefer — the direct call IS\n   the synced path.\n3. **A third gate that is not a widget at all:** `SetAutoArm` returns silently\n   unless `MpSync.Configurable(pawn)`. Pre-checked, so a dead pawn is refused\n   rather than reported applied against a write that never happened.\n\nRECONCILIATION DEBT AT MERGE, flagged by the worker and confirmed: `SeekVerbs.cs`\nnow on main has its own `SeekMod` reflection cache, and `FswaBridge.cs` has\nanother. They solve the same problem with different code. **One shared mod-interop\nhelper is owed**, and `FswaBridge`'s is the better of the two (signature+return\nbinding, typed delegates, `bool?` reads) — fold `SeekMod` into it rather than the\nreverse.\n\nSTILL OWED: in-game acceptance. `accept/auto-arm-lever.md` has five phases of\nenvelopes ready to drive. The worker could not run any of them — it never\nlaunched the game, correctly. Phase 1's single highest-value check is simply\nwhether `AccessTools.TypeByName(\"FSWA.AutoArmTracker\")` resolves on the live\nbench; nothing downstream means anything until it does. Phase 2.7 sweeps the\njournal for `[AutoRimmer] FSWA` warnings — **one of those appearing means the\nbridge is fabricating and the whole run is suspect, green checks included.**","files":null}]}