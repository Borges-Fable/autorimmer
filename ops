{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788274247,"nonce":"BCFAkeeidhBFumOvbZmMxEp8004=","message":"MERGED to main — `25b65ff`, rebuilt in `5be9037`. **NOT CLOSED, and the reason\nis the acceptance, not the code.**\n\nEvery bullet on this issue that can be settled by reading or compiling is\nsettled. Every bullet that says \"on a bench\" has not been run. `state:doing`\nstands until it has.\n\n\n- `dotnet build -c Release` at the merged tree: **0 warnings, 0 errors.**\n- The merged tree's `*.cs` is **byte-identical** to the branch tip's, checked\n  rather than assumed. The branch carried no assembly, so the rebuild was owed\n  unconditionally; it landed alone, with the pdb path naming this worktree\n  under `obj/Release/`, the Release configuration blob present and the Debug\n  blob absent, `InformationalVersion` = `1.0.0+25b65ff` (this HEAD), and\n  598,402 differing bytes at +24K — a real IL change, not a no-op rebuild.\n- `accept/s13-mod-surface.py --selftest`: **25/25**, and the runner says so\n  itself — *\"the ASSERTIONS work. Nothing about the mod was asserted; take this\n  to a bench.\"* That sentence is the whole caveat on this merge.\n- Verb surface: **119 → 120**, the one addition being `site-survey`.\n\n\n**No part of this ran against a live game.** Not one verb has been called. The\nsuite additions are assertions that have never seen a response envelope, which\nis precisely the state session 13 was in before its bench pass turned five\n\"should work\" claims into evidence — and session 13's own experience is that\nlive testing produced findings no amount of source reading would have.","files":null}]}