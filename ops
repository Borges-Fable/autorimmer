{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788116285,"nonce":"Nve5vkRSsIbc5bPFg9A1gnIaeo4=","message":"PROGRESS — orchestrator session 1 (2026-08-30). Wave 0 done bar one line; HARD\nGATE reached, stopping for Dorian.\n\n- **0.1 (3fa4cf5)** — spike complete, verified personally, branch merged to main\n  fast-forward (26b638c..57263e7). Bench `_RimWorld-Agent` exists and boots:\n  38 active mods, **0 red errors / 9 known-benign warnings**, isolation proven\n  via `Mono path[0]`, 60.8 tps sustained with the window parked and unfocused.\n  FINDINGS.md (439 lines) committed. Full evidence in that issue's verification\n  comment; every number re-derived from raw artifacts rather than trusted.\n  **NOT state:done** — one acceptance line (Xvfb demo) is blocked on a package\n  install needing Dorian's password. Left `state:next`.\n\nFINDINGS conclusions are on **8 of the 14** wave 1-3 specs — 1.1, 1.2, 1.3,\n1.4, 2.1, 2.3, 2.4, 3.1. The other six (2.2, 2.5, 3.2, 3.3, 3.4, 3.5) have\nnothing in FINDINGS that touches them; that is a decision, not an omission.\nHeaviest amendment is 1.3's — the spike's whole reason for existing.\n\nThe three that change how a spec gets built:\n1. **1.3's loop shape is settled.** Budgeted `DoSingleTick` inside\n   `GameComponentUpdate`, yielding every frame, game left Paused. Verified\n   against decompiled `Game.UpdatePlay`/`Map.MapUpdate`: the slot runs after the\n   frame's MapUpdates, so nothing per-frame needs re-implementing. Recommended\n   `max_tps` default **1000** at a 25ms/frame budget, plus a hwmon thermal\n   governor — this chassis plateaus at 93.4-94.1C under any sustained full-duty\n   load, with about 1C of headroom.\n2. **A parked bench is not a running bench.** Hyprland delivers no frames to a\n   hidden special workspace, and RimWorld's tick loop is frame-bound: the bench\n   sat at 1-2 fps ~= 4 tps while looking healthy in `ps`. Fixed in\n   `run-agent.sh`; every later spec should assert a tick delta, not process-up.\n3. **A symlinked engine stub re-roots the game at the Steam install** via\n   `/proc/self/exe`, silently loading Steam's Mods/ (~40 TypeLoad errors from a\n   modlist that was fine). Fixed. `_RimWorld-Test` has the same latent bug,\n   untouched, flagged for Dorian.\n\n1. **Install `xorg-server-xvfb x11vnc`** to close 0.1's last acceptance line\n   (then one 3-min run), or waive it.\n2. **Confirm the modlist deviation**: Guests and RegisterLanes were added to the\n   v1 bench. DESIGN's \"Own vanilla+DLC mods\" list omits both, but justifies the\n   whole visitor cluster as required *by Guests*, and Factions/Guests is the M3\n   target — a bench with the cluster and not the mods it serves tests nothing.\n   Also added: CashRegister (transitive dep). Dropped as unnecessary: HugsLib\n   (nothing declares it), PerspectiveShift (SeekAndKill verified standalone,\n   source and runtime). FINDINGS section 2.\n\n**1.1 (097f33a)**, agent:fable — its deps are 0.1 only, and it gates every other\nwave-1+ spec. 1.4 is the natural parallel second (different repo, no file\noverlap) but it depends on 1.1's protocol, so it waits.","files":null}]}