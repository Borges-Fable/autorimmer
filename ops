{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788224816,"nonce":"z3IyMSDaWvsyNESXmik7YdjBcus=","message":"GREEN — closed on live acceptance, session 11 (`65b03c2`), 2026-08-31.\n\nThis issue said a gate-refused order writes no action row, leaving half of `4087644`'s journal rule unimplemented. **Measured live: it does write one.**\n\n`accept/4087644-order-honesty.py` passed **all 100 checks**, including the ones that are this issue's subject:\n\n- `6.5` — the JOURNAL ALONE shows the refused emergency orders (`extinguish`, `beat-fire`, `man-turret`, `tend`), read back off `data.events` with the verb at `payload.verb`.\n- `6.1b`–`6.4b` — each refusal returned `journal_seq \u003e= 1`, which only happens when `PawnActs.ActOn` → `Act` → `Journal.Emit(\"action\", …)` wrote a row.\n- `6.15c` — a gate-refused `attack` journals.\n- `3.4`'s `5.12c`, on the same bench: an UNDRAFTED doctor refused by the drafted-only gate STILL wrote an action row carrying its verdict.\n\nWhy this looked unimplemented for so long: the only check that read the journal for these rows dug `data.rows`/`data.entries` instead of `data.events`, and read `verb` flat instead of `payload.verb`. Both misses returned an empty list, which is indistinguishable from 'no rows were written'. The behaviour was shipped; the instrument was pointed at the wrong key.","files":null}]}