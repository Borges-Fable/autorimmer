{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788183275,"nonce":"+nXZqRMw9Ev8tMIQkitpwcHAFzo=","message":"HANDOVER — orchestrator session 7 (dorian's Linux box), 2026-08-31.\nEverything pushed: main at ffbe95f, both spec branches, all bug refs.\n\n**15 of 25 done.** 1.8 closed and merged. 3.4 verified with caveats and left\nOPEN on purpose. One new issue filed.\n\nTHE THING THE LAST HANDOVER DID NOT KNOW: the two boxes have disjoint\ntoolchains. BORGES has no python; this box has no pwsh and there is none in the\nArch repos. 3.4's acceptance shipped as PowerShell, so on the box that now owns\nthe bench it could not run at all. `accept/3.4-pawn-orders.py` now exists\nalongside the .ps1 — same check ids, same raw file protocol, no `rwa`\ndependency. Keep BOTH. Whoever writes the next acceptance should write it in\npython, since the bench is here.\n\n1.8 (b8785e8) — CLOSED, merged fast-forward at 0079e00, 109 PASS / 0 FAIL\n\n`fa11564` was sound and its \"unverified draft\" acceptance needed no correction —\nonly the exec bit changed. The regression test is the whole result: under 1.3's\nloop we ticked through a force-pausing dialog and starved\n`OpenAutomaticLetters`; under 1.8 a first timing-out letter halted the advance\nat its own open tick (41202 vs 41202) and a SECOND one arrived and opened at\n41801 vs 41801. Evan's argument is now measured. The speed ladder is exact\n(60/180/360/900 nominal -\u003e 59.99/179.99/359.84/899.78 measured) and REPLACES\n1.3's tps table. Two SKIPs, both honest: an external pause needs a human on the\nspace bar, and `PlayerCanControl` is unreachable from the protocol.\n\n3.4 (39c9db7) — 130 of 148, STILL state:next. Do not close.\n\nBullets 1 and 4 fully passed. Bullet 2 did not run (my stockpile siting — see\nbelow). Bullet 3 half-ran: the policy stripped the pawn but it slept before\nre-dressing. Zero red errors across two full runs. Full breakdown on the issue.\n\nTWO THINGS TO PICK UP FIRST\n\n- **A real defect.** `warden {mode:\"AttemptRecruit\", recruitable:true}` evaluates\n  the mode gate against the BEFORE state and applies `recruitable` after, so the\n  mode silently does not take — and 3.4's own acceptance makes exactly that call.\n  Fix the lever ordering, or re-evaluate the gate after the lever pass.\n- **A spec gap, filed as e8f2c32 (p2, backlog).** `work-priorities` correctly\n  refuses priority 1 or 2 with manual priorities off, but nothing in the verb\n  surface can turn them on (`playSettings.useWorkPriorities`) and they default\n  OFF. 8 of 3.4's 18 failures are unreachable state. 3.4's acceptance cannot\n  pass on a colony the agent staged itself until this is resolved.\n\nFIXTURE LESSON THAT COST A WHOLE RUN\n\nI loaded `Autosave-5.rws` after diffing its 39 required mods against the bench's\n39 — zero missing, compatibility verified not assumed. I did not check whether\nthe colony was ALIVE. It was in a mood death spiral: 4 of 5 colonists downed,\nand the surgery patient DIED mid-run. All 44 failures of that run traced to it.\n**A compatible save is not a viable save — read the pawns, not just the modIds.**\nIt still proved the gates under adversity: every verb refused cleanly and\n`assign` on the dead pawn returned a clean bad-args naming the id.\n\nBENCH STATE — read this before launching\n\n- **`autostart.rws` IS ARMED and this time it is deliberate.** Quicktest map\n  staged with `dev:starter-kit survival`: 3 healthy colonists, research bench,\n  32 startable projects, Anesthetize addable, saved at tick 1061. A known-good\n  fixture, not a leftover.\n- **It needs one fix before 3.4 re-runs:** the stockpile is sited where the\n  colonists idle, so phase 2's steel spawns INSIDE it at `pawn:A` and needs no\n  hauling. Re-site it well away from them.\n- `Prefs.xml` changed from 640x480 windowed to 2560x1600 fullscreen so Evan\n  could watch. Backup at `Prefs.xml.bak-640x480`; restore for unwatched runs.\n- The launcher re-applies its `special:rwagent` windowrule each boot; `--no-rule`\n  skips it.\n\nNEW HAZARD FOR DESIGN: `Dialog_NamePlayerFactionAndSettlement` force-paused the\ngame mid-run and `advance` returned `reason:\"dialog\"`. A naming dialog on the\nordinary play path is a 1.7-class wedge not previously listed, and is concrete\nevidence for 3.5 being M1-critical.\n\nORCHESTRATOR DEBT STILL OWED: factor the four private `Act` emitters into one\nhelper — AreaVerbs, DesignationVerbs, ZoneVerbs (3.2) and PawnActs (3.4). Both\nworkers left the same comment saying the orchestrator owns it at merge. They\ndiffer in one behaviour: 3.2 drops null-valued extras, 3.4 keeps them. Also\nstill owed from the 3.4 merge: fold `Pawn_ReadingTracker` into `PawnSafe.Policies`.\n\nRECOMMENDED ORDER: 3.4 (warden fix + re-site + re-run), then 1.9 (p1, blocks\n3.3), then 3.5 and 3.6. **2.5 is unblocked** — it needed dorian's box and the\nwork is now on it.","files":null}]}