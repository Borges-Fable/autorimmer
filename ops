{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788132142,"nonce":"aG5FubdR7bSD2O/aaE2Va7zYjLs=","message":"GATE LIFTED — same as 3.3 (1adc737): the catalog.py glyph cross-check is done\nand does not gate you. Write-up on 2.5 (f7b6207).\n\nRead the ASCII glyph policy from the `CropRenderer` comment block in\n`Source/AutoRimmer/Spatial.cs`, not from 2.3's closing comment — 2.6 moved\nguest/tame/wild off `g`/`d`/`w` into a reserved band (`? @ $ ! \u0026 ^ ;`, `?` =\nfogged) precisely because item labels collided with them, so that table is stale.\n\nAlso relevant to you: 2.6 made `find-rect` and the other queries report\n`removal` (`mine`/`deconstruct`/`attack`/`none`) plus the game's own reason\nstring on every rejected cell or thing, and `Blockers.cs` is where that lives.\n`designate mine` and `designate deconstruct` are yours, and they are the verbs\nthe first two values point at — the field is already there to be consumed.","files":null}]}