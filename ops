{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788268014,"nonce":"BnQhc+nKeamGEu50GhYC2HPGHds=","message":"Companion finding, session 13: `3a5ff6c`. Slide is one of two ways placement lies; **wipe is the other.** `dev:spawn-thing` in `mode:\"direct\"` for a building passes `canWipeEdifices:true` to `GenSpawn.CanSpawnAt` and then spawns with `WipeMode.VanishOrMoveAside`, whose `WipeExistingThings` calls `Destroy(DestroyMode.Vanish)` on every wall, rock or building in the footprint — no refund, no journal line, reported as plain success. On the bench (`20260901T121508`) the refused `HiTechResearchBench` at `[125,128]` would, had its interaction cell been clear, have erased the granite at `[124,128]` silently.\n\nTwo asks that belong with this issue's call-site audit rather than in a second one:\n- Extend the \"every `GenPlace`/`GenSpawn` call site with its mode\" closing list to include the WIPE mode per site. Today: `DevVerbs.SpawnThing` (VanishOrMoveAside, gated), `WorldFixtureVerbs.Bench` (Vanish, ungated), `JournalVerbs.SpawnPlayerBuilding` (Vanish, ungated).\n- The `displaced` field this issue adds and the `wiped[]` field `3a5ff6c` adds are the same idea — \"what the game did that you did not ask for\" — and should sit beside each other in the result.\n\nAlso for your convention section: `find-rect`'s `at` is a corner, `GenAdj.OccupiedRect` places about a centre, and for even sizes `CellRect.CenterCell` is one cell off from the placement centre. Exact-or-refuse needs to say which cell \"exact\" is measured against; `c718e4a` publishes the game's `pos` so the answer can be \"that one\".","files":null}]}