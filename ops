{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788972783,"nonce":"n5qcj1y37U7636tcTdji4lUwAto=","message":"DEPENDENCY DISCHARGED — the shared route this issue needs now EXISTS. git-bug `d318d4a` shipped `Source/AutoRimmer/UsableSafe.cs` + `UseVerbs.cs` on branch `worktree-agent-af1fc4174ec68e485` (`a5bccbe`). Per that issue's scoping pass, `826d4bf` is now a strict dependent: **do not re-transcribe the CanBeUsedBy chain or the StartJob reproduction — extend the file.**\n\nWHAT IS ALREADY DONE FOR YOU\n* All nine `CompUsable.CanBeUsedBy` clauses, each with its own gate name — `UsableSafe.Refusals(comp, pawn, stopAtFirst)`. Your correction 2 (\"carried\" is not a game rule) is honoured: reach and reserve are the gates, possession is not.\n* Your MISS 1 (the `Dialog_MessageBox` on `TryStartUseJob`) is solved for both issues: `use` reproduces the local `StartJob()` and never calls `TryStartUseJob`. `UsableSafe.MakeJob(comp, pawn, extraTarget)` is that reproduction and ALREADY takes an extra target — `JobMaker.MakeJob(useJob, parent, extraTarget)` when valid, with vanilla's `if (extraTarget == pawn) -\u003e Invalid` clause. `UsableSafe.Confirmations` pre-collects the ConfirmMessage set (and `CompUsableImplant`'s separate royal-title modal).\n* Your MISS 3 (three validators, not one): the USER gate is done. The TARGET gate is yours.\n\nWHAT IS LEFT, AND IT IS ONLY THIS\n1. The `target` argument on `use`. It is currently REFUSED by `VerbArgs.RefuseStray` naming this issue, so a caller cannot silently lose it — remove it from that refusal and add it to `PawnActs.UseArgs` when you implement it.\n2. Seat `CompTargetable.selectedTarget` and `caster` (both private; `PostExposeData` scribes `selectedTarget`, so this is a write to SAVE state).\n3. Reproduce `CompTargetable.ValidateTarget` + each subclass override + `GetTargetingParameters().CanTarget(target)` — `DoEffect` re-checks the latter INDEPENDENTLY, which is your MISS 2.\n4. Assert the corpse is GONE and a living pawn exists, not that the job was accepted.\n\nTHE HOOK IS ALREADY IN PLACE. `UsableSafe.TargeterRoute` detects a `CompTargetable` whose `PlayerChoosesTarget` is true (read by reflection off the protected abstract property, not from a class list) and returns gate **`needs-target`** naming this issue. So today `use {pawn, thing:\u003cMechSerumResurrector\u003e}` refuses cleanly instead of taking a job that would complete and resurrect nobody. Your `target` argument turns that refusal into the act; nothing else in the chain moves.\n\n`UsableSafe.PlayerChoosesTarget(ct)` is public and returns TRUE when the getter cannot be bound — i.e. it fails toward refusing, never toward a silent no-op.\n\nTwo smaller notes from the d318d4a pass: `CompUsable.ValidateTarget` (the gizmo route's validator, distinct from `CompTargetable.ValidateTarget`) gates on `pawn.IsPlayerControlled` first; and `CompTargetable.OrderForceTarget` sets `selectedTarget` then calls `TryStartUseJob` — i.e. the ONE public setter routes straight back into the modal this route exists to avoid, so seating the field and calling `UsableSafe.MakeJob` yourself is the shape to use.","files":null}]}