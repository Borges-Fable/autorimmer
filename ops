{"author":{"id":"e0b5a45ee94df92761e1991b03f72f45cac91cde564e2faac1223bf52a3fe292"},"ops":[{"type":3,"timestamp":1788196892,"nonce":"rZe1y1eIFXODEz7mkU16xaf0nIk=","message":"PRE-MERGE AUDIT of `spec/auto-arm-lever`, against FSWA's own IL (decompiled from\nthe bench copy at `_RimWorld-Agent/Mods/FindSuitableWeaponAndAmmo/Assemblies/`)\nand the decompiled 1.6 source. VERDICT: merge after the fixes now committed at\n`addd6d3`. Release build clean, 0 warnings 0 errors. Not run in game.\n\nTHE DIAGNOSIS SURVIVED SOURCE VERIFICATION, including all three of the worker's\ncorrections to this issue's own API notes. Verified by member name:\n\n- `AutoArmTracker.IsAutoArm(Pawn)` static bool and `SetAutoArm(Pawn,bool)` static\n  void both exist with those exact signatures; `AutoArmTracker.Get` is the static\n  property, `Current.Game?.GetComponent\u003cAutoArmTracker\u003e()`.\n- `PawnColumnWorker_AutoArm.HasCheckbox` is `pawn.IsColonist \u0026\u0026\n  !pawn.WorkTagIsDisabled((WorkTags)8)`. ILSpy could not resolve the enum name;\n  `Verse/WorkTags.cs` gives `Violent = 8`, so the worker's reading is right.\n  `FSWA_Columns.xml` names `FSWA.PawnColumnWorker_AutoArm` and\n  `FSWA_AssignTable.xml` inserts it into the Assign table, so the \"this is a\n  column of the tab `assign` already reproduces\" argument holds literally.\n- `Patch_PawnGizmos.Postfix` draws on `IsColonistPlayerControlled ||\n  MechUtility.IsWeaponUsableMech(pawn)` and `Disable`s on `!RaceProps.IsMechanoid\n  \u0026\u0026 WorkTagIsDisabled(Violent)`. The union collapses correctly to `IsColonist`\n  for non-mechs: `Pawn.IsColonistPlayerControlled` requires `IsColonist`\n  (Verse/Pawn.cs), so it is a strict subset and the verb's gate is the wider,\n  correct one.\n- `MpSync.Configurable` is `pawn != null \u0026\u0026 !pawn.Dead \u0026\u0026 !pawn.Destroyed`.\n- `MpSync.RegisterAll` does register `SetAutoArm(Pawn,bool)` itself, via\n  `MpBridge.Register`, one of six. Correct as the worker said.\n- The forbidden claim is real: `FSWA_MapComponent.CollectCandidates` excludes\n  `ForbidUtility.IsForbidden(thing, Faction.OfPlayer) || FireUtility.IsBurning`.\n- No namespace collision: the CE assembly is entirely `FSWA.CE.*`, so\n  `AccessTools.TypeByName(\"FSWA.AutoArmTracker\")` has one candidate.\n- Observer safety confirmed at the bottom: `Verse/Game.cs GetComponent\u003cT\u003e()` is a\n  plain scan over `components` with no lazy creation, and `IsAutoArm` is a\n  HashSet lookup. The read really is safe for the observer surface.\n\nFOUR DEFECTS, FIXED IN `addd6d3`.\n\n1. RULE 2 HAD A SECOND MOUTH, AND IT WAS OPEN ON THE OBSERVER PATH.\n   `FswaBridge.cs AutoArmOf` guarded only the throw. But `IsAutoArm` is\n   `Get?.optedIn.Contains(pawn) ?? false` — a MISSING `AutoArmTracker`\n   GameComponent answers the legal value `false` and never throws. The write path\n   already caught that (read-back disagrees, `WriteDiagnosis` names the null\n   tracker); `pawn {sections:[\"area\"]}` did not, and would have published\n   \"auto-arm is off\" for a game whose tracker is gone. A bare `false` is now\n   probed against `AutoArmTracker.Get` and downgraded to null-with-a-reason on a\n   DEFINITE absence only, since turning every honest \"not opted in\" into UNKNOWN\n   would be the same lie facing the other way.\n\n2. THE OPTIONAL PROBES COULD DISABLE THE LEVER THEY ARE DOCUMENTED AS UNABLE TO\n   COST. `AccessTools.PropertyGetter(tracker,\"Get\")` and the `MechUtility` bind\n   sat inside the outer try, whose catch nulls `readAutoArm`/`writeAutoArm`. Both\n   carry comments promising they cost \"the explanation, not the lever\". They now\n   have their own guard, so they cannot.\n\n3. `accept/auto-arm-lever.md` PHASE 4.4 HAD THE DROP CLAIM BACKWARDS, AND IT WAS\n   LOAD-BEARING. It said \"a colonist's own drop lands UNFORBIDDEN\n   (`Pawn_EquipmentTracker.TryDropEquipment` does not forbid)\". The signature is\n   `TryDropEquipment(ThingWithComps eq, out ThingWithComps resultingEq, IntVec3\n   pos, bool forbid = true)` and it calls `resultingEq.SetForbidden(forbid,\n   warnOnFail: false)`; `RimWorld/JobDriver_DropEquipment.cs` — the driver behind\n   `drop`'s `JobDefOf.DropEquipment` — passes three arguments, so forbid defaults\n   TRUE. An ordered drop lands FORBIDDEN, FSWA excludes it, the pass ends at \"no\n   candidate weapons on the map\", and phase 4 reads as a broken lever. 4.5 is now\n   marked REQUIRED rather than \"belt and braces\". (The one vanilla path that does\n   un-forbid is `Pawn_EquipmentTracker.MakeRoomFor`, which explicitly calls\n   `dropped.SetForbidden(value: false)` — so staging with an `equip` instead of a\n   `drop` would behave the other way. Named in the note.)\n\n4. THE PHASE 4 FIXTURE TABLE WAS WRONG BY OMISSION — session 8's lesson, again.\n   `FSWA_MapComponent.CollectEligiblePawns` requires Spawned, on this map, not\n   Dead, not Downed, `MentalStateDef == null`, not packing a caravan, not Drafted\n   (default `allowSwapWhileDrafted == false`), no equip job already in flight,\n   and for humanlikes `IsColonist \u0026\u0026 !WorkTagIsDisabled(Violent) \u0026\u0026\n   CapableOf(Manipulation)`. Every one of those silently drops the pawn from the\n   arming pass while `auto_arm` still reads `true`. None were in the table. Added,\n   with the cadence — `IsHashIntervalTick(map, evalIntervalTicks)`, default 600,\n   plus a 7500-tick forced floor, and `SetAutoArm` calls `MarkPawnDirty` on\n   opt-in — so 4.7's 5000 ticks is justified rather than guessed.\n\nTWO THINGS RECORDED, NOT CHANGED.\n\n- WHY THIS COPIES A GATE WHERE `seek-at-will` CALLS ONE. That verb's rule is\n  better and is not available here: `HasCheckbox` is a `protected` INSTANCE\n  member whose only game-configured instance is `PawnColumnDef.Worker`, and that\n  getter CONSTRUCTS the worker and writes `workerInt`/`workerInt.def` on first\n  read (`RimWorld/PawnColumnDef.cs`) — PawnSafe Class A, reached from an observer\n  path on a bench where the Assign tab may never have been opened. The gizmo's\n  clause is inline in a Harmony postfix iterator, so there is no member at all.\n  Written into `AutoArmGate`'s header so nobody \"fixes\" it into a hazard.\n- THE SAVE-XML SECOND READER IS PARTLY INVALIDATED HERE. `AutoArmTracker\n  .ExposeData` runs `Purge()` during the SAVING pass, dropping every entry whose\n  pawn is null/Dead/Destroyed — the same write-on-SAVE class as `Bill.ExposeData`\n  in DESIGN's 2026-08-31 entry. The XML is a valid second reader for a LIVE\n  pawn's opt-in and is not one for a dead pawn's. Noted in the accept file.\n\nMERGE RISK, MEASURED RATHER THAN ASSUMED. `DESIGN.md` DOES CONFLICT with current\nmain — a trial `git merge origin/main` gives `CONFLICT (content) in DESIGN.md`\nand nothing else, because main's own three session-8 entries and this branch's\ntwo both append at EOF. Additive; resolve by keeping both blocks in date order.\nAgainst the two branches auditing in parallel the collisions are textually\ndisjoint: `spec/stable-pawn-order` touches `PawnSerializer.cs` at ~line 139 (the\nroster) where this branch touches `Area()` at ~line 913;\n`spec/manual-work-priorities` touches `PawnManageVerbs.cs` at lines 35-320 (the\n`work-priorities` verb) where this branch touches `Assign` at 438-800. Both will\nconflict in DESIGN.md for the same EOF reason. This branch does not touch\n`accept/3.4-pawn-orders.*`, which the other two both do.\n\nSTILL OWED, AND ONLY THE ORCHESTRATOR CAN: everything at runtime. Phase 1's\nsingle highest-value check is unchanged — whether `AccessTools.TypeByName\n(\"FSWA.AutoArmTracker\")` resolves on the live bench — and 2.7's sweep for\n`[AutoRimmer] FSWA` warnings now also catches the new fabricated-read guard.","files":null}]}