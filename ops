{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788970474,"nonce":"GiE7sm5KUbRvSvIlZw7aQIz9EIE=","message":"Scope correction found while checking the mech-boss path: **the target set is not just items — it is every `CompUsable`, buildings included.**\n\n`CallBossgroupUtility.TryStartSummonBossgroupJob` (decompiled 1.6) ends in\n\n    list[num].TryGetComp\u003cCompUsable\u003e().TryStartUseJob(pawn, null, forced);\n\non a `MechbandDish`. So summoning a mech boss is the same verb as using a neurotrainer, reached through a different picker. A `use` verb scoped to `ThingCategory.Item` would miss it.\n\nTwo more defs that make this p1 rather than a nicety, both `CompProperties_Usable` + `CompProperties_UseEffectInstallImplant` with **no surgery recipe anywhere**, so `surgery-add` cannot reach them:\n\n- `Mechlink` (Biotech/Defs/HediffDefs/Hediffs_Mechanitor.xml) — the entire mechanitor tree hangs off it.\n- `PsychicAmplifier` (Core/Defs/HediffDefs/Hediffs_Psycasts.xml) — one psylink level per use.\n\nBoth are in Dad's catalog on this bench (500 and 2600). Without this verb they can be bought and never used.","files":null}]}