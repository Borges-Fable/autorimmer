{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":1,"timestamp":1788350814,"nonce":"/+2i2gz1F9ctQ80k7VmGh1cRh6s=","title":"Run `m1-20260901`: **\"0 in stockpiles\" was misread as \"none\" three separate times.** The lesson graduated into the playbook as [[stockpile-scope-hides-your-own-supplies]] and it should not have needed to, because the mod already knows the right shape and uses it once.","message":"`resources.food_rot` publishes both a stockpile-scoped and a map-wide figure with an explicit `scope` note, precisely because the difference is load-bearing. `steel`, `wood`, `meds` and `components` publish one number, stockpile-scoped, with no twin and no note. The agent that reads `steel: 0` cannot tell an empty colony from a colony whose steel is lying in the open where it was mined.\n\n`DigestVerb.cs:540` already carries the comment explaining why `food_rot` has the twin. The other rows were never given it.\n\n\n1. Every `resources.*` row publishes a map-wide figure alongside the stockpile-scoped one, in the shape `food_rot` already established, with the same kind of `scope` note. Do not invent a second vocabulary.\n2. The forbidden/unforbidden split is visible where it matters: material lying on the ground forbidden is not available in the sense the caller means. `7e8c969`'s finding that 800 **forbidden** silver counted in full toward a trade figure is the precedent for getting this wrong in the other direction.\n3. `checklists/turn.md` §materials-designation-loop cites the map-wide field, and the playbook lesson is amended to name it rather than describe the workaround.\n4. `accept/\u003cid\u003e-resource-scope.py` on a bench: drop material outside any stockpile, assert the stockpile figure and the map-wide figure differ and both are published; haul it in, assert they converge.\n\n\nC3 of `next-run.md`, the steel bottleneck that runs through the entire causal chain of the wipe, was read through this field every turn.","files":null}]}