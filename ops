{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":1,"timestamp":1788112101,"nonce":"4izyvUQ4jeNPHqpgrlIGfowLSDA=","title":"2.1 Colony digest + what-changed","message":"Wave 2 — eyes. Depends: 1.1, 1.2. Exemplar for 2.2/2.4 (they follow its pattern).\n\n## Goal\nThe glance: one verb giving the agent the player's peripheral vision, sized\nfor LLM context (~1-2KB pretty). Plus what-changed since last glance.\n\n## Context (read first)\n- DESIGN.md §Observation model (four layers, budgets)\n- decompiled: AlertsReadout (active alerts ARE the attention model — reuse),\n  Alert_LowFood (food-days math), colonist bar sources, ResourceReadout\n- 1.2 journal (what-changed rides on it)\n\n## Scope\n`digest` returns:\n- time: date, season, weather, outdoor temp; game speed/tick\n- alerts: active, with severity + short label (verbatim from readout)\n- colonists: one line each — name, current job, mood% + trajectory arrow,\n  health flag (bleeding/infection/tend-needed), draft state, location room\n- resources: food-days (vanilla math), meds, key stocks (steel/wood/silver),\n  power: generation vs draw + battery days\n- threats: hostile count + kind if any\n- changed: journal event counts by type since the given seq (args.since)\nStable field names — rwtest and the play skill both consume this forever.\n\n## Invariants\nRead-only; bounded cost (this gets called constantly); size budget enforced\n(truncation rules explicit, e.g. alerts cap with \"+N more\").\n\n## Open questions (worker's)\nExactly what makes the cut at budget (propose, then validate against real play\nsessions in 4.3 and iterate); mood trajectory window; room-name source.\n\n## Acceptance\n- Under budget on a 5-pawn colony with 6+ active alerts.\n- Alerts match the in-game readout exactly; food-days within 10% of\n  hand-computed; power figures match the power tab.\n- Two calls with args.since correctly bracket the events between them.","files":null}]}