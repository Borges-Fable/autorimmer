{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788270732,"nonce":"gi0xoxdeZULtZN9ZGHrXLN+1QRo=","message":"THE PROMISE IN MY CLOSE COMMENT IS DISCHARGED — `ab1321e`, same day.\n\nThat comment said the missing shape check was \"handed to the geometry cluster\nof the current round as a one-line shape check rather than left as a note\nhere.\" The round had not started, so the check sat in a CLOSED issue with no\nowner — which is precisely the orphan shape session 13 wrote up when it found\nall four M1 lessons cited by no checklist. Recording an obligation somewhere\nnobody reads is the failure, not the forgetting.\n\n`accept/e6faa51-channel-alphabet.py`, 12 PASS / 0 FAIL, no bench: `transcripts/`\nbanks real `map-view` and `map-dump` envelopes, and this is a check about a\nFIELD, so banked envelopes are the right input.\n\nIt turned out bigger than the one line I promised, and better:\n\n- **Bullet 1** is asserted by KEY EXISTENCE on both channels, so a regression\n  that dropped the field FAILs rather than passing as `eq(None)` — the trap\n  `accept/`'s own convention exists for, and the reason this was worth writing.\n- **Bullet 2** is asserted as the mutual naming rather than as sameness: the\n  two alphabets must DIFFER, and each must name the other in `distinct_from`.\n  That is what lets a consumer detect a cell-for-cell comparison it should not\n  be making.\n- **Bullet 3 is now ENFORCED, not conventional**, which is the part my close\n  comment recorded as a residual. Each envelope's identity is checked against\n  the constant its own source declares (`Spatial.cs`'s `AlphabetId`,\n  `MapDumpVerbs.cs`'s literal), and `MapDumpVerbs`' `distinct_from` must cite\n  `Spatial.cs`'s constant verbatim — so an id bumped in one place and not the\n  other fails here instead of shipping two truths. Both identities are also\n  required to carry a trailing version, since an unversioned name gives a\n  stale consumer nothing to compare.\n\nNothing about the close changes; the issue stays closed. This comment exists so\nthe residual it named does not outlive it.","files":null}]}