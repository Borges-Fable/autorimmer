{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788304427,"nonce":"rkdsjAwrsVEHVlc5R18OyzgPiRk=","message":"THE SUITE'S FIRST BENCH RUN: 290 PASS / 4 FAIL / 0 SKIP of 294, and NONE of the four failures is this issue's half. Branch `fix/s21-suite-failures`, commits `a6b6777`, `8d17ccf`, `4c17009` off `2bf4539`. Log at `accept/runs/s21-20260901/suites/61794cd.log`. **Nothing on the branch has met a bench.**\n\nEvery check in phases 1 and 2 — the bleed-out clock, the null-both-ways read on one pawn, the three-way `game_shows_clock` gate, the Captain shape with `BloodLoss` at row 0 through a real truncation, and the arithmetic cross-check at 1.7 — **passed on the first live run against a bench.** So did 6.3a–6.3d, the \"one builder, two callers\" proof that `triage`'s clock IS `pawn.health.bleedout` byte for byte. That is this issue's whole surface, measured.\n\nThe four failures split two to `40ed42f` and one to `722c951`, with the fourth a suite bug in the seam between them:\n\n| check | verdict | where |\n|---|---|---|\n| 6.4c / 6.4d | **SUITE bug** — asserted `no-bed` on `casualties[0]`, which this fixture makes the STANDING bleeder, for whom `CanRescueNow` → `WantsToBeRescued`'s `!pawn.Downed` answers `cannot-rescue` before the bed lookup is ever reached | `40ed42f` |\n| 7.2h | **MOD defect** — `enabled_but_incapable` was emitted only when non-empty, in two files. Fixed; the key is now an always-present, possibly empty list | `40ed42f` |\n| 7.6a | **SPEC ambiguity, RULED** — the casualty halt is on the TRANSITION, not on the state. The mod is right; the check was asserting a state | `722c951` |\n\nFull write-ups are on those two issues and the three rulings are in DESIGN's decisions log.\n\n## What is still owed here, unchanged\n\n**The in-game text comparison.** Comment #4 marked it ORCHESTRATOR-MANUAL and it stays that way: a suite cannot read RimWorld's UI, so 1.1j / 1.3c / 1.6d assert the reproduced `HealthCardUtility` gate three ways and the suite never claims it compared the text. The numbered recipe is a section of `accept/61794cd-bleed-triage.md`. That is the one acceptance bullet of this issue neither the suite nor any bench pass has discharged.\n\nThe `coma` outcome and the `(Deathless)` branch remain unstageable — no `dev:` verb adds a gene, which is `ff1f0b9`'s neighbourhood — and `deathless_gene` is asserted false throughout so the branch stays unexercised rather than silently assumed.\n\n## Two suite changes that touch this issue's checks\n\n- `--selftest` is 66 checks now (was 63): 9.8d, 9.11a and 9.12a re-derive the two session-21 fixes out of the source, so a regression fails offline rather than on a bench.\n- The re-run needs a **Release rebuild first** for `40ed42f`'s half (7.2h, 7.2i, 3.3q, 3.3a assert the new shape); this issue's own checks are unaffected either way.","files":null}]}