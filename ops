{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788274247,"nonce":"Qfao7B1uncn8FBHiQeiJuV0j+qk=","message":"Landed as the three commits this issue was split into: `7e23e7f` `SiteGate`,\n`90e578e` `site-survey`, `923800b` `find-rect {def}`.\n\n**`find-rect {w,h}` is provably untouched** — `923800b` is 340 insertions and\n**zero deletions** in `SpatialVerbs.cs`, with `def` branching at the top. The\nAcceptance asked for \"byte-for-byte what it was\"; a zero-deletion diff is a\nstronger proof than a comparison run would have been, and cheaper.\n\nResolutions, all cited by member and all in DESIGN's decisions log: the `rot`\ndefault diverges per verb deliberately (`dev:spawn-thing` stays `Rot4.North`\nper `DebugThingPlaceHelper.DebugSpawn`'s overload; the siting reads use\n`def.defaultPlacingRot` per `Designator_Build` — **76 vanilla defs set it\nnon-North**, so unifying would silently re-face every building the shipped\nsuites stage); `Rot4.FromString` is unusable on agent input because it\n`Log.Error`s on anything unrecognised and is case-sensitive; the corner→centre\ninverse is verified against `GenAdj.OccupiedRect` on every call and refuses\nrather than returning a displaced centre; `site-survey`'s overlay is a second\ngrid rather than extra glyphs, because mixing them would oblige a bump of\n`map-view/ascii-1` and `map-dump`'s `distinct_from`, which\n`accept/e6faa51-channel-alphabet.py` now enforces.\n\n**Owed on a bench, and it is most of this Acceptance:** the granite replay with\n`verdict.reason` being the game's `InteractionSpotBlocked` sentence; the chair\ntolerated on an interaction cell; two benches refused with\n`InteractionSpotOverlaps`; the rotation search finding an East candidate in a\ntwo-deep corridor where nothing fits North; and the round-trip that pins\n`AdjustForRotation`'s even-axis shift — spawning at each candidate's `pos` and\nreading `OccupiedRect()` back equal to its rect, for all four rotations of an\neven-sized def. None of that has been run.","files":null}]}