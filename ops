{"author":{"id":"e0b5a45ee94df92761e1991b03f72f45cac91cde564e2faac1223bf52a3fe292"},"ops":[{"type":3,"timestamp":1788215714,"nonce":"WQ4JKNqynD0viK46RZCuM8H5AeQ=","message":"RULINGS (orchestrator, BORGES G2, run fbb2c59) — resolving the verification\ncomment's open decisions before dispatch, so the worker builds to a settled\ncontract.\n\n1. SCOPE: the port covers the `things` DETAIL list (`detail:true`) and\n   `FireScan` — on BOTH routes (`fires` verb and `things`' `data[\"fire\"]`).\n   `rollups` stays as it is: keyed by `def`, which is a stable key; it has no\n   `thingIDNumber` to sort by; and re-ordering a summary whose position is the\n   message would remove information, not add it. This adopts the verifier's own\n   recommendation — the fires case is an agent dispatching firefighters, which\n   is precisely a caller holding handles, and the cap of 12 churns membership.\n\n2. ACCEPTANCE, amended per the verification: the two-reads stability check is\n   pinned with `detail_cap \u003e= things_total` (or asserted on the intersection).\n   Above the cap, membership moves with the score BY DESIGN (2.6's rule: a cap\n   must never hide the urgent item), and a failure there is the correct\n   outcome, not a bug.\n\n3. VOCABULARY: exactly the pawns contract as extracted in comment #1 —\n   argument words `id`/`attention`; published `order` values\n   `id-asc`/`attention-desc`; `selected_by` publishes the selection rule;\n   `attention_rank` computed on the ranking BEFORE the cut; unknown `order`\n   raises VerbArgsException, never a silent fallback; the class comment\n   carries the sequence-not-membership honesty note. For `fires`,\n   `selected_by` states its real rule (`outside-home-then-size-desc`).\n\nDispatched to opus (own worktree, BORGES).","files":null}]}