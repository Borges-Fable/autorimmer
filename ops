{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788279230,"nonce":"Rc317wP0DrLJRvA+i65RI7+AQfM=","message":"LANDED — `159812f`, merged `e9c47ea`. The steps chain, and the audit list's\nomission is closed.\n\n**The either/or your verification comment called NOT TESTABLE is resolved by\nkeeping the field and making it true.** `data.bench.expect_bills` now reflects\nthe bills present when the call RETURNS. And `expect_slots_free` went with it —\nthe acceptance names only `expect_bills`, so an implementation could have\nsatisfied the text to the letter and still reported 15 free slots on a bench\nholding two bills. Your comment caught that; it is fixed rather than argued.\n\n**The `MaxCount` question is answered as REFUSE, not clear.** `BillStack.AddBill`\nvalidates nothing (`bill.billStack = this; bills.Add(bill);`), so the 15-bill cap\nis UI-only and the model would happily exceed it. The step refuses at the cap;\n`clear_bills:true` opts into clearing. Silently clearing a caller's bills to make\nroom is the same class of defect this issue is about — a fixture quietly staging\nsomething other than what was asked for.\n\n**`BillUtility.MakeNewBill` replaces `new Bill_Production(recipe)`**, so a recipe\nusing an unfinished thing gets `Bill_ProductionWithUft` rather than the wrong\nruntime type. That was point 3 of your comment and is not in the body.\n\n**`open-letter` — the omission — is fixed with `bill`.** Your reading was right\nthat the body's audit sentence names four steps that CREATE and omits the one\nthat RESOLVES; a worker auditing exactly those four would have closed the class\nwith it still open.\n\n\n`JOURNAL.md` documents the journal's event types, and the **`action` type\nshipped in spec 3.2 and was never documented** — five verbs have been writing an\nundocumented type for four sessions. Both missing rows added.\n\n\nNothing has run against a game. The acceptance's own test — `{steps:[\"bench\",\n\"bill\"]}` run TWICE on one map leaving two benches with two bills each, never\none bench with four — is the bench pass, and it is cheap. Note that the `bench`\nstep now runs a placement gate it never had (see `3a5ff6c`), so a refusal there\nwould be a shipped fixture breaking rather than a chaining bug; check them in\nthat order.","files":null}]}