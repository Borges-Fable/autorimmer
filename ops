{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788302262,"nonce":"pcfNECASQiw+tv7V0MoH4vi01Qg=","message":"CORRECTION TO MY OWN REPORT, AND IT CHANGES THE FIX. I wrote, in the body above: \"Nothing in the envelope said anything was wrong. `ok: true`, and the only field that hints at it is `ticks_elapsed: 187541`.\" **That is false.** I had not opened the `until` sub-object. It says:\n\n    \"until\": {\n      \"kind\": \"condition\", \"path\": \"time.tick\", \"op\": \"\u003e=\", \"value\": 949,\n      \"edge\": true,\n      \"true_when_armed\": true,\n      \"saw_false\": false,\n      \"first_false_tick\": null,\n      \"observed_last\": 188400,\n      \"evaluations\": 419\n    }\n\n`true_when_armed: true`, `saw_false: false`, `first_false_tick: null`. **The mod diagnosed this completely and said so in the envelope**, and session 19 put that field there for exactly this case — DESIGN's own entry reads \"The advance reports `true_when_armed`, so a caller that asked a question already answered can see that it did.\" It anticipated this and instrumented it.\n\nAlso mine to withdraw: option 3 of the Scope, \"consider publishing `armed_true`\", **already ships under the name `true_when_armed`.** Delete that option.\n\n## What the defect actually is, restated\n\nNot \"the mod cannot tell.\" **The mod computes at arm time that the predicate is already satisfied, publishes that fact, and then accepts an unbounded advance anyway.** It has everything it needs to refuse or to bound, and acts on none of it.\n\nThat is a narrower bug and a cheaper fix than the body implies, because no new observation is required — only enforcement keyed on a field that already exists:\n\n1. **At arm time, if `true_when_armed` is true and `edge` is true and no `timeout_ticks` was given, REFUSE** — the halt is unreachable by construction and the mod already knows it. The refusal can quote the three fields it already computes.\n2. **Default `timeout_ticks` to something finite** so the same call with a timeout supplied, or with the default applied, terminates.\n3. ~~publish `armed_true`~~ — already shipped as `true_when_armed`.\n\nThe acceptance bullets stand as written, with one addition: **the refusal must be derived from `true_when_armed`, not from a second computation of the same thing**, so the field and the enforcement cannot disagree.\n\n## And a second thing the envelope shows, which is not a defect\n\n`evaluations: 419` over `frames: 6287` at `eval_ms_avg: 0.0172` — total `eval_ms_total: 7.19 ms` across three in-game days. Session 19's predicate-cost decision is holding up: the polling is free. The problem is purely that nothing bounds it.\n\nI am leaving the original body unedited above rather than rewriting it, because a filed report that quietly changes its own evidence is worse than one carrying a correction — and the specific thing I got wrong (asserting silence without opening a sub-object) is worth having on the record next to the finding.","files":null}]}