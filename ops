{"author":{"id":"e0b5a45ee94df92761e1991b03f72f45cac91cde564e2faac1223bf52a3fe292"},"ops":[{"type":3,"timestamp":1788140069,"nonce":"hOiZTdFmR22fytGIjGYH/g9JamQ=","message":"CLOSED — acceptance verified personally by the orchestrator on the BORGES\nbench, session 5 round 2. Merged to main fast-forward (aa523d7..cfe8c08 incl.\none orchestrator fix), Build commit on main after the 2.4 merge. Bench\nsessions sid 20260831T011639 (quicktest) and 20260831T012110 (the autostart\nfixture boot).\n\n**All three acceptance bullets demonstrated**\n\n1. Scripted fixture end-to-end: one `dev:starter-kit` call staged 3 named\n   colonists (Ada/Bo/Cyn) with exact skills/passions/needs, 10 item stacks\n   (survival preset + extras, all placed in full), 2 research projects\n   finished, and a 16MB save — then `dev:incident RaidEnemy 400 letter:false`\n   fired 5 Yttakin raiders with no letter and no force-pause, and the\n   following advance ran clean (`reason:\"ticks\"`, never `\"dialog\"`). The\n   dry-run resolved the same plan first and mutated nothing (`journal_seq\n   null, provenance not-applicable`).\n2. Complete dev provenance: 26 dev journal events for the run; every seq in\n   the kit's `caused_journal_seqs` present in the journal, every individual\n   result's `dev.journal_seq` matching a line — the join runs both directions.\n3. Spawned things visible to 2.x: the kit colonists and the raiders appeared\n   in `pawns` under the right classes with the ids the verbs reported; Ada's\n   `pawn` skills read back exactly (Construction 10/Major, Mining 8/Minor;\n   unspecified passions untouched); the digest saw `danger:High, hostiles:5`.\n   Later (round 2's 2.4 acceptance) the dev layer built an entire walled\n   bedroom sited by 2.3's find-rect that 2.4's room analysis classified —\n   three waves exercising each other in one chain.\n\n**Per-verb smoke, live:** wounded-and-standing damage (5 injuries, not\ndowned); heal full 5→0 hediffs; set-need honest about Need_Seeker drift\n(`volatile:true` + instant + rates for Mood, false for Food); add/remove\nhediff; berserk start/stop; weather Clear→Rain; finish-research Electricity;\nunfog (37 cells, honest before/after); destroy; guest-status on a live raider\n(Guest→Prisoner via the real SetGuestStatus path, resistance/will rolled);\nbad-args with did-you-mean suggestions ('steel' → Steel, MineableSteel…);\npositions via `pawn:\u003cid\u003e`. **The autostart path demonstrated end to end**: a\nplain (non-quicktest) launch booted straight into the starter-kit's saved\nfixture in ~20s at the save's tick — the no-UI fixture route is real.\nautostart.rws deleted afterwards so later boots are not hijacked.\n\n**One defect found by acceptance, fixed by the orchestrator, re-verified\nlive (cfe8c08):** `dev:faction-goodwill relation:` called `SetRelationDirect`\non a goodwill-using faction — vanilla Log.Errors AND no-ops there, which was\nthe run's only red error. The relation path now drives goodwill to the band's\nfar value for goodwill factions, and permanent enemies are refused up front\nwith the reason. Re-verified: `relation:ally` on a neutral outlander moved\ngoodwill 0→74 with honest after-values (74, one shy of Ally — the faction's\nown natural-goodwill cap; the game's ruling, reported not hidden), the\npermanent-enemy refusal named the def, and the session ended **zero red\nerrors**.\n\n**Not demonstrated, in those words:** `dev:spawn-pawn` for a hostile faction\n(the raid incident covered hostile arrival; the verb's lord-join path for a\ndirectly spawned raider was not exercised live); `dev:destroy` modes other\nthan vanish; `dev:add-hediff`'s severity argument; weather's mechanical\neffects after the 4000-tick lerp. `dev:guest-status guest` on a factionless\n(wild) pawn would NRE into a caught `exception` result rather than a clean\nbad-args — cosmetic, noted for a future minor pass.\n\nOpen-question resolutions are the worker's comment above; all six reviewed\nand accepted. JOURNAL.md's dev row now records 3.1's ownership, the additive\npayload fields, and the `dev.journal_seq` join (orchestrator doc commit).","files":null}]}