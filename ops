{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788276027,"nonce":"wQiK2c5kOPh3TCFUoKbw/rGzlA0=","message":"BENCH PASS DONE — CLOSING. `_RimWorld-Agent`, `--quicktest`, assembly `25b65ff`,\n2026-09-01. Full record: `accept/runs/s15-20260901/`.\n\n**The replay, with a wall rather than granite.** Wall on the interaction cell of\na bench asked for at `[130,135]`:\n\n    asked for:  [130, 135]\n    reason:     \"Interaction spot is blocked by wooden wall.\"\n    cell_role:  \"interaction\"\n    blocker:    Wall / wall at [130, 134] | removal: \"deconstruct\"\n\nAgainst the banked failure this issue was filed on\n(`accept/runs/s13-20260901/results/accs13-026-devspawnthing.json`): reason named\ngranite, blocker named a `WoodLog` on the TARGET cell, `removal: \"none\"`.\n\nSubstituting a wall for granite is deliberate and stronger, not a shortcut: it\nexercises the same `InteractionCellStandable` branch while producing a\nDIFFERENT removal class (`deconstruct`, not `mine`), so it proves `Blockers.At`\nis being asked about the refusing cell rather than returning a constant. The\ntarget cell's own occupant appears nowhere in the row.\n\n**Formal coverage, live:** `1.3s` `data.failed.0.cell`, `1.3t` \"with no branch\nto name, the refusing cell IS the target\", `1.3u` \"…and `cell_role` says so\ninstead of naming a tier\". Suite 174 PASS / 0 FAIL.\n\n**Said plainly: the added checks for the OFF-target case did not run.** `1.5h-j`\nare gated behind a fixture the pass did not create — check 1.5's NOTE reads \"the\nresearch bench PLACED on that cell (direct mode wipes with\n`WipeMode.VanishOrMoveAside`), so the `CanSpawnAt` branch was not exercised\", and\n`1.6l-m` needed a `--near-cell` argument. So the interaction case is proven by\nthe hand replay above and NOT yet by an automated check. The gap is the fixture,\nnot the fix — and it is worth closing next time someone is on a bench, because a\nhand replay does not survive a regression.\n\nBonus, and it is this issue's own defect one level up: session A found\n`GenSpawn.CanSpawnAt` has SEVEN branches where `WhyNoSpawn` reproduced six —\n`GenConstruct.NotBlockingAnyInteractionCells` runs there too. Proven live: a\nfootprint placed over a neighbour's interaction cell refused with *\"Simple\nresearch bench would block simple research bench's interaction spot.\"* Before,\nthat came back as \"ThingDef.CanSpawnAt refused\" with no cell at all.","files":null}]}