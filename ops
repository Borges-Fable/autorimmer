{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788268013,"nonce":"2/YYLhdWe26Zt08VvtIR2PEl0DE=","message":"Scope note from `c718e4a` (site-survey, session 13), and the picture ruling it records in DESIGN's decisions log (2026-09-01).\n\nThe question was whether a site survey's \"picture\" is structured JSON, an ASCII crop, or this PNG channel. Ruling: **all three consumers are served, from one source of truth, and the PNG stays where this spec's invariant puts it.** The structured survey is the contract; an ASCII crop rides in the same result (mid-loop, no round trip, `map-view`'s alphabet); and the PNG remains a pure function of dump + catalog rendered CLI-side — so this spec gains, when it lands, **an optional overlay input to `rwa render` that is exactly `site-survey`'s tiers**: footprint / interaction / margin cells, drawn over the rect at 2–3x the footprint. Nothing image-shaped goes in the mod.\n\nTwo things that follow for your worker:\n- The overlay needs an alphabet/identity of its own beside `channel.alphabet` so a stale overlay is detectable (`e6faa51`'s rule; the survey publishes `CropRenderer.AlphabetId` plus an overlay id).\n- Your acceptance's \"an agent shown ONLY the PNG answers room count, door locations, which room holds the stove\" gets a fourth question once the overlay exists: \"why was this site refused\" — answerable from the picture because the refusing cell is marked.","files":null}]}