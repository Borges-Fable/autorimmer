{"author":{"id":"127a58c7f014820991be95805df42525db12c92e2d7319d04d4df105a9aa3444"},"ops":[{"type":3,"timestamp":1788301259,"nonce":"tLG7RsmWaaYckVkY3UGZT+ckgoY=","message":"THE SAVE/LOAD BULLET IS MET, and the last acceptance bullet on this issue is now green. Session 21, orchestrator. Envelopes `08`/`09` at `accept/runs/s21-20260901/posture/`, commit `01f8245`.\n\nThe bullet reads \"the posture survives a save/load round trip, **or the digest says it did not**.\" **It survives**, so the fallback branch never came up.\n\nSaved with `journal-selftest {steps:[\"save\"], save_name:\"s21-posture\"}` (`GameDataSaveLoader.SaveGame`), 14,470,877 bytes. Bench killed, relaunched **without `--quicktest`** through the autostart route — deliberately, because `run-agent.sh` refuses `--quicktest` alongside an `autostart.rws` and is right to (the `Root.checkedAutostartSaveFile` race, `c8c0199`). New session `20260901T221926`.\n\n| field | before save | after load |\n|---|---|---|\n| `ok` | true | **true** |\n| `will_seek` | 1/1 | **1/1** |\n| `area_bound` | 3/3 | **3/3** |\n| `attack` | 1/1 | **1/1** |\n| `on_contact` | flee 2, attack-then-seek 1 | **identical** |\n| `flee_risk` | `[]` | **`[]`** |\n| `areas` | `[\"Area 2 x3\"]` | **`[\"Area 2 x3\"]`** |\n\nRead back per pawn from `posture` after the load: all three still bound to Area 2, Teresa still `Attack` with seek on resolving to `attack-then-seek`, the two violence-incapable still `Flee`.\n\n## The third that actually needed testing\n\nTwo of the three were predictable and the worker said so: area membership is `Scribe_Collections` by reference, hostility is `Scribe_Values` with `Flee` as both the default and the field initialiser. Vanilla pawn state, expected to persist.\n\n**Seek is not ours.** It lives in SeekAndKill's own GameComponent as `SK_SeekPawns`, with `PruneStaleIds` dropping absent ids on load. The worker read that and correctly declined to claim it, because a third-party mod's GameComponent surviving a round trip is not something source reading settles — `PruneStaleIds` could have dropped a live pawn, or the component could have been keyed on something the reload renumbers. It did not. `will_seek 1/1` after the load, on the same pawn, is the measurement.\n\nThat is the difference between \"all three persist, read from the scribe calls\" and \"all three persisted, on this bench, across this restart\" — and it is the reason this bullet was written as a bench bullet.","files":null}]}